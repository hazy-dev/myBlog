<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.study.blog.mapper.AuthMapper">
	
	<select id="auth_list" parameterType="hashmap" resultType="Auth">
	
		SELECT IDX, ID, NAME, AUTH_GROUP FROM ADMIN
	
	</select>
	
	<update id="update_auth" parameterType="hashmap">
	
		UPDATE ADMIN SET AUTH_GROUP = #{auth_group} WHERE IDX = #{idx}
	
	</update>
	
	<select id="user_check" parameterType="hashmap" resultType="int">
	
		SELECT COUNT(*) FROM ADMIN WHERE ID = #{user_id}
	
	</select>
	
	<insert id="user_insert" parameterType="hashmap">
	
		INSERT INTO ADMIN ( ID, PWD, AUTH, NAME, AUTH_GROUP ) 
		VALUES ( #{user_id} , #{user_password} , #{max_auth}, #{nickname}, #{user_auth_selectbox} )
	
	</insert>
	
	<select id="max_auth" resultType="int">
	
		SELECT MAX(AUTH) AS CNT FROM ADMIN
	
	</select>
	
</mapper>